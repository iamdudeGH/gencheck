# virtualenvs (the deployed site installs its own deps)
.venv/
.venv-*/
venv/

# local working artifacts — screenshots, log dumps, raw receipt dumps.
# NOT published: a receipt dumped from studio-dev on 9 Sept 2026 included a
# validator's node_config, which at that time carried a private_key field.
# The RPC no longer returns one (re-checked 11 Sept 2026), but nothing in here
# is needed by the repo, the portal, or the deploy — keep it local.
artifacts/

# A local scrape of the GenLayer docs site (docs.genlayer.com). It is someone
# else's documentation, not this project's source, and it is 860 KB of it.
# Nothing in the repo, the portal, or the deploy reads it.
full-documentation.txt

__pycache__/
*.py[cod]
*.egg-info/

# setuptools residue from building the package locally
build/
dist/
.pytest_cache/

.env
.env.*
*.log
portal.err

.DS_Store
Thumbs.db
